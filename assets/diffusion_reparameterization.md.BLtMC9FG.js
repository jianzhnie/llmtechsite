import{c as a,a7 as e,y as i,B as h}from"./chunks/framework.3cjvm90C.js";const $=JSON.parse('{"title":"重参数：从正态分布到 Gumbel Softmax","description":"讲解重参数化技巧在连续与离散分布中的应用，涵盖正态分布重参数和 Gumbel Softmax 方法。","frontmatter":{"title":"重参数：从正态分布到 Gumbel Softmax","description":"讲解重参数化技巧在连续与离散分布中的应用，涵盖正态分布重参数和 Gumbel Softmax 方法。"},"headers":[],"relativePath":"diffusion/reparameterization.md","filePath":"diffusion/reparameterization.md","lastUpdated":1782447613000}'),l={name:"diffusion/reparameterization.md"};function m(s,t,n,p,r,b){return e(),i("div",null,[...t[0]||(t[0]=[h(`<h1 id="重参数-从正态分布到-gumbel-softmax" tabindex="-1">重参数：从正态分布到 Gumbel Softmax <a class="header-anchor" href="#重参数-从正态分布到-gumbel-softmax" aria-label="Permalink to “重参数：从正态分布到 Gumbel Softmax”">​</a></h1><h2 id="基本概念" tabindex="-1">基本概念 <a class="header-anchor" href="#基本概念" aria-label="Permalink to “基本概念”">​</a></h2><p><strong>重参数（Reparameterization）</strong>实际上是处理如下期望形式的目标函数的一种技巧： $$ \\begin{equation}L_{\\theta}=\\mathbb{E}<em>{z\\sim p</em>{\\theta}(z)}[f(z)]\\label{eq:base}\\end{equation} $$</p><p>这样的目标在 VAE 中会出现，在文本 GAN 中也会出现，在强化学习中同样会出现（$f(z)$ 对应于奖励函数）。取决于 $z$ 的连续性，它对应不同的形式： $$ \\begin{equation}\\int p_{\\theta}(z) f(z)dz,,,\\text{(连续情形)}\\qquad\\qquad \\sum_{z} p_{\\theta}(z) f(z),,,\\text{(离散情形)}\\end{equation} $$ 当然，离散情况下我们更喜欢将记号 $z$ 换成 $y$ 或者 $c$。</p><p>假设我们需要对 $L_{\\theta}$ 求关于 $\\theta$ 的梯度。如果函数 $f_\\theta(z)$ 本身关于 $\\theta$ 梯度存在，则 $$ \\begin{aligned} \\nabla_\\theta \\mathbb{E}<em>{p(z)}\\left[f</em>\\theta(z)\\right] &amp; =\\nabla_\\theta\\left[\\int_z p(z) f_\\theta(z) d z\\right] \\ &amp; =\\int_z p(z)\\left[\\nabla_\\theta f_\\theta(z)\\right] d z \\ &amp; =\\mathbb{E}<em>{p(z)}\\left[\\nabla</em>\\theta f_\\theta(z)\\right] \\end{aligned} $$ 实际上，上面的推导说明梯度和期望符号可以交换运算顺序，即期望的梯度等于梯度的期望。那么问题来了：如果密度函数 $p$ 也有参数 $\\theta$ 呢？我们尝试重复上面的步骤： $$ \\begin{aligned} \\nabla_\\theta \\mathbb{E}<em>{p</em>\\theta(z)}\\left[f_\\theta(z)\\right] &amp; =\\nabla_\\theta\\left[\\int_z p_\\theta(z) f_\\theta(z) d z\\right] \\ &amp; =\\int_z \\nabla_\\theta\\left[p_\\theta(z) f_\\theta(z)\\right] d z \\ &amp; =\\int_z f_\\theta(z) \\nabla_\\theta p_\\theta(z) d z+\\int_z p_\\theta(z) \\nabla_\\theta f_\\theta(z) d z \\ &amp; =\\underbrace{\\int_z f_\\theta(z) \\nabla_\\theta p_\\theta(z) d z}<em>{\\text {What about this? } }+\\mathbb{E}</em>{p \\theta(z)}\\left[\\nabla_\\theta f_\\theta(z)\\right] \\end{aligned} $$</p><p>由于第一项不能写作某个函数关于 $p_\\theta(z)$ 的期望，Monte Carlo 方法在这里无法适用——因为它要求我们从 $p_\\theta(z)$ 中采样。如果 $\\nabla_\\theta p_\\theta(z)$ 有解析解，那么一切问题将迎刃而解。但现实中 $\\nabla_\\theta p_\\theta(z)$ 往往无法写出。了解了问题所在后，接下来我们看看使用重参数化技巧能否有所改善。 $$ \\begin{aligned} \\boldsymbol{\\epsilon} &amp; \\sim p(\\boldsymbol{\\epsilon}) \\ \\mathbf{z} &amp; =g_\\theta(\\boldsymbol{\\epsilon}, \\mathbf{x}) \\ \\mathbb{E}<em>{p \\theta(\\mathbf{z})}\\left[f\\left(\\mathbf{z}^{(i)}\\right)\\right] &amp; =\\mathbb{E}</em>{p(\\boldsymbol{\\epsilon})}\\left[f\\left(g_\\theta\\left(\\boldsymbol{\\epsilon}, \\mathbf{x}^{(i)}\\right)\\right)\\right] \\ \\nabla_\\theta \\mathbb{E}<em>{p \\theta(\\mathbf{z})}\\left[f\\left(\\mathbf{z}^{(i)}\\right)\\right] &amp; =\\nabla</em>\\theta \\mathbb{E}<em>{p(\\boldsymbol{\\epsilon})}\\left[f\\left(g</em>\\theta\\left(\\boldsymbol{\\epsilon}, \\mathbf{x}^{(i)}\\right)\\right)\\right] \\ &amp; =\\mathbb{E}<em>{p(\\boldsymbol{\\epsilon})}\\left[\\nabla</em>\\theta f\\left(g_\\theta\\left(\\boldsymbol{\\epsilon}, \\mathbf{x}^{(i)}\\right)\\right)\\right] \\ &amp; \\approx \\frac{1}{L} \\sum_{l=1}^L \\nabla_\\theta f\\left(g_\\theta\\left(\\epsilon^{(l)}, \\mathbf{x}^{(i)}\\right)\\right) \\end{aligned} $$ 上述推理是理解 VAE 的关键——我们使用重参数化技巧将期望（1）的梯度表示为梯度（5）的期望。如果 $g_\\theta$ 是可微的，就可以使用蒙特卡洛方法来估计 $\\nabla_\\theta \\mathbb{E}<em>{p</em>{\\boldsymbol{\\theta} }(\\mathbf{z})}\\left[f\\left(\\mathbf{z}^{(i)}\\right)\\right]$。</p><p>为了最小化 $L_{\\theta}$，我们需要将其明确写出来，这意味着要从 $p_{\\theta}(z)$ 中采样。而 $p_{\\theta}(z)$ 带有参数 $\\theta$，如果直接采样就会丢失 $\\theta$ 的梯度信息，从而无法更新参数。Reparameterization 提供了一种变换，使我们能从 $p_{\\theta}(z)$ 中采样的同时保留 $\\theta$ 的梯度。（注：如果考虑最一般的形式，$f(z)$ 也应带上参数 $\\theta$，但这不增加本质难度。）</p><h2 id="连续情形" tabindex="-1">连续情形 <a class="header-anchor" href="#连续情形" aria-label="Permalink to “连续情形”">​</a></h2><p>简单起见，我们先考虑连续情形： $$ \\begin{equation}L_{\\theta}=\\int p_{\\theta}(z) f(z)dz\\label{eq:lianxu}\\end{equation} $$</p><p>其中 $p_{\\theta}(z)$ 是具有显式概率密度表达式的分布，在变分自编码器中常见的选择是正态分布。</p><h3 id="形式" tabindex="-1">形式 <a class="header-anchor" href="#形式" aria-label="Permalink to “形式”">​</a></h3><p>从式(3)中知道，连续情形的$L_{\\theta}$实际上就对应一个积分，所以，为了明确写出$L_{\\theta}$，有两种途径：最直接的方式是精确地完成积分(3)，得到显式表达式，但这通常都是不可能的了；所以，唯一的办法是转化为采样形式(1)，并试图在采样过程中保留$\\theta$的梯度。</p><p>重参数就是这样的一种技巧，它假设从分布$p_{\\theta}(z)$中采样可以分解为两个步骤：</p><p>(1) 从无参数分布$q(\\varepsilon)$中采样一个$\\varepsilon$；</p><p>(2) 通过变换$z=g_{\\theta}(\\varepsilon)$ 生成$z$。那么，式(1)就变成了 $$ \\begin{equation}\\mathbb{E}<em>{z\\sim \\mathcal{N}\\left(z;\\mu</em>{\\theta},\\sigma_{\\theta}^2\\right)}\\big[f(z)\\big] = \\mathbb{E}<em>{\\varepsilon\\sim \\mathcal{N}\\left(\\varepsilon;0, 1\\right)}\\big[f(\\varepsilon\\times \\sigma</em>{\\theta} + \\mu_{\\theta})\\big]\\end{equation} $$</p><p>这时候被采样的分布就没有任何参数了，全部被转移到$f$内部了，因此可以采样若干个点，当成普通的 loss 那样写下来了。</p><h3 id="例子" tabindex="-1">例子 <a class="header-anchor" href="#例子" aria-label="Permalink to “例子”">​</a></h3><p>一个最简单的例子就是正态分布：对于正态分布来说，重参数就是$\\mathcal{N}\\left(z;\\mu_{\\theta},\\sigma_{\\theta}^2\\right)$中采样一个$z$ ”变成“从$\\mathcal{N}\\left(\\varepsilon;0, 1\\right)$ 中采样一个$\\varepsilon$，然后计算$\\varepsilon\\times \\sigma_{\\theta} + \\mu_{\\theta}$”，所以： $$ \\begin{equation}\\mathbb{E}<em>{z\\sim \\mathcal{N}\\left(z;\\mu</em>{\\theta},\\sigma_{\\theta}^2\\right)}\\big[f(z)\\big] = \\mathbb{E}<em>{\\varepsilon\\sim \\mathcal{N}\\left(\\varepsilon;0, 1\\right)}\\big[f(\\varepsilon\\times \\sigma</em>{\\theta} + \\mu_{\\theta})\\big]\\end{equation} $$ 如何理解直接采样没有梯度而重参数之后就有梯度呢？其实很简单，比如我说从$\\mathcal{N}\\left(z;\\mu_{\\theta},\\sigma_{\\theta}^2\\right)$中采样一个数来，然后你跟我说采样到 5，我完全看不出 5 跟 $\\theta$ 有什么关系（求梯度只能为 0）；但是如果先从$\\mathcal{N}\\left(\\varepsilon;0, 1\\right)$ 中采样一个数比如 0.2，然后计算$0.2 \\sigma_{\\theta} + \\mu_{\\theta}$ ，这样我就知道采样出来的结果跟 $\\theta$ 的关系了（能求出有效的梯度）。</p><p>我们首先生成一个均值为 0，标准差为 1 的高斯分布 $\\mathcal{N}(0,1)$。在这个分布中采样数据 $\\epsilon$，经过一个变换后得到目标分布：$Z=\\mu + \\sigma \\epsilon$。$\\mu$ 为均值，$\\sigma$ 为标准差，由网络计算得出。伪代码如下：</p><div class="language-python line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">def</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> reparametrization</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(z_mean, z_log_var):</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    epsilon </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> K.random_normal(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">shape</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">K.shape(z_mean))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> z_mean </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> z_log_var </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> epsilon</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>此时，返回的变量就具有了梯度，而采样的过程在整个计算图之外，采样的 $\\epsilon$ 就是一个常量，此时便可以梯度下降。</p><h3 id="回到-vae-中" tabindex="-1">回到 VAE 中 <a class="header-anchor" href="#回到-vae-中" aria-label="Permalink to “回到 VAE 中”">​</a></h3><p>在 VAE 中，ELBO（evidence lower-bound）写作： $$ \\begin{aligned} \\operatorname{ELBO}(\\boldsymbol{\\theta}, \\boldsymbol{\\phi}) &amp; =\\left[\\mathbb{E}<em>{q</em>\\phi(\\mathbf{z})}\\left[\\log p_\\theta(\\mathbf{x}, \\mathbf{z})-\\log q_\\phi(\\mathbf{z} \\mid \\mathbf{x})\\right]\\right] \\ \\end{aligned} $$ 这里，我们区分了模型参数 $θ$ 和隐变量（latent variable）$\\phi$。那么梯度表示为： $$ \\begin{aligned} \\operatorname{ELBO}(\\boldsymbol{\\theta}, \\boldsymbol{\\phi}) &amp; =\\underbrace{\\nabla_{\\theta, \\phi}\\left[\\mathbb{E}<em>{q</em>\\phi(\\mathbf{z})}\\left[\\log p_\\theta(\\mathbf{x}, \\mathbf{z})-\\log q_\\phi(\\mathbf{z} \\mid \\mathbf{x})\\right]\\right]}<em>{\\text {Gradient w.r.t. } \\phi \\text { over expectation w.r.t. } \\phi} \\end{aligned} $$ 在我们假设先验和后验估计均为高斯分布的时候，上面的式子可以进一步化简成： $$ \\nabla</em>{\\theta, \\phi} \\mathcal{L}^B=-\\nabla_{\\theta, \\phi} \\overbrace{\\left[\\mathrm{KL}\\left[q_\\phi\\left(\\mathbf{z} \\mid \\mathbf{x}^{(i)}\\right) | p_\\theta(\\mathbf{z})\\right]\\right]}^{\\text {Analytically compute this } }+\\nabla_{\\theta, \\phi} \\overbrace{\\left[\\frac{1}{L} \\sum_{l=1}^L\\left(\\log p_\\theta\\left(\\mathbf{x}^{(i)} \\mid \\mathbf{z}^{(l)}\\right)\\right)\\right]}^{\\text {Monte Carlo estimate this } } $$ 从 encoder-decoder 角度看 ELBO 的话： $$ \\mathcal{L}^B=-\\mathrm{KL}[\\overbrace{q_\\phi\\left(\\mathbf{z} \\mid \\mathbf{x}^{(i)}\\right)}^{\\text {Encoder } } \\overbrace{p_\\theta(\\mathbf{z})}^{\\text {Fixed } }]+\\frac{1}{L} \\sum_{l=1}^L \\log \\overbrace{p_\\theta\\left(\\mathbf{x}^{(i)} \\mid \\mathbf{z}^{(l)}\\right)}^{\\text {Decoder } } $$</p><p>$$ \\begin{array}{rlrl} &amp; \\boldsymbol{\\mu}_x, \\boldsymbol{\\sigma}_x=M(\\mathbf{x}), \\Sigma(\\mathbf{x}) \\quad \\text { Push } \\mathbf{x} \\text { through encoder } \\ &amp; \\epsilon \\sim \\mathcal{N}(0,1) \\quad \\text { Sample noise } \\ &amp; \\mathbf{z}=\\boldsymbol{\\epsilon} \\boldsymbol{\\sigma}_x+\\boldsymbol{\\mu}_x \\quad \\text { Reparameterize } \\ &amp; \\mathbf{x}<em>r=p</em>\\theta(\\mathbf{x} \\mid \\mathbf{z}) \\ \\mathbf{z} &amp; =\\boldsymbol{\\epsilon} \\boldsymbol{\\sigma}_x+\\boldsymbol{\\mu}_x &amp; &amp; \\text { Reparameterize } \\ \\mathbf{x}<em>r &amp; =p</em>\\theta(\\mathbf{x} \\mid \\mathbf{z}) &amp; &amp; \\text { Push z through decoder } \\ \\text { recon. loss } &amp; =\\operatorname{MSE}\\left(\\mathbf{x}, \\mathbf{x}_r\\right) &amp; &amp; \\text { Compute reconstruction loss } \\ \\text { var. loss } &amp; =-\\operatorname{KL}\\left[\\mathcal{N}\\left(\\boldsymbol{\\mu}_x, \\boldsymbol{\\sigma}_x\\right) | \\mathcal{N}(0, I)\\right] &amp; \\text { Compute variational loss } \\ \\mathrm{L} &amp; =\\text { recon. loss }+\\text { var. loss } &amp; &amp; \\text { Combine losses } \\end{array} $$</p><h3 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to “总结”">​</a></h3><p>让我们把前面的内容重新整理一下。总的来说，连续情形的重参数还是比较简单的：连续情形下，我们要处理的$L_{\\theta}$实际上是式(3)，由于精确的积分我们没有办法显式地写出来，所以需要转化为采样，而为了在采样的过程中得到有效的梯度，我们就需要重参数。</p><p>从数学本质来看，重参数是一种积分变换，即原来是关于$z$积分，通过$z=g_{\\theta}(\\varepsilon)$变换之后得到新的积分形式，</p>`,27)])])}const f=a(l,[["render",m]]);export{$ as __pageData,f as default};
