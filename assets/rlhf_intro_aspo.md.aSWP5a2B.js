import{c as a,a7 as t,y as r,B as o,v as n,D as e}from"./chunks/framework.3cjvm90C.js";const _=JSON.parse('{"title":"ASPO：非对称重要性采样策略优化","description":"介绍ASPO算法如何通过非对称重要性采样改进策略优化的稳定性","frontmatter":{"title":"ASPO：非对称重要性采样策略优化","description":"介绍ASPO算法如何通过非对称重要性采样改进策略优化的稳定性"},"headers":[],"relativePath":"rlhf/intro/aspo.md","filePath":"rlhf/intro/aspo.md","lastUpdated":1782448598000}'),p={name:"rlhf/intro/aspo.md"};function l(i,s,g,c,h,P){return t(),r("div",null,[...s[0]||(s[0]=[o(`<h1 id="aspo-非对称重要性采样策略优化" tabindex="-1">ASPO：非对称重要性采样策略优化 <a class="header-anchor" href="#aspo-非对称重要性采样策略优化" aria-label="Permalink to “ASPO：非对称重要性采样策略优化”">​</a></h1><p>在训练大型语言模型（LLMs）的主流强化学习算法中，如 PPO 和 GRPO，都依赖于 PPO-Clip 组件。我们发现其原始设计中存在一个关键缺陷：<strong>对具有正优势（Positive Advantage）的 token 存在权重失配</strong>，这会导致模型过拟合、熵坍缩和重复输出，最终造成训练<strong>过早收敛到局部最优</strong>。为了解决这一问题，我们提出了<strong>非对称双重裁剪（Asymmetric Dual-Clipping）</strong>，这是一种重新设计的裁剪策略。实验表明，该策略能有效防止过早收敛，提高训练稳定性，并显著提升 LLM 的性能。</p><p>👨‍💻 <a href="https://github.com/wizard-III/Archer2.0" target="_blank" rel="noreferrer">Github</a>, 🤗 <a href="https://huggingface.co/Fate-Zero/Archer2.0-Code-1.5B-Preview" target="_blank" rel="noreferrer">HF Model</a>, 🤗 <a href="https://huggingface.co/datasets/Fate-Zero/Archer2.0-Code-1.5B" target="_blank" rel="noreferrer">HF Dataset</a>, 🌐 <a href="https://zhuanlan.zhihu.com/p/1950989602023244983" target="_blank" rel="noreferrer">知乎</a>, 📖 <a href="https://arxiv.org/abs/2510.06062" target="_blank" rel="noreferrer">论文</a></p><h2 id="深入理解-llm-训练中强化学习的关键见解" tabindex="-1">深入理解 LLM 训练中强化学习的关键见解 <a class="header-anchor" href="#深入理解-llm-训练中强化学习的关键见解" aria-label="Permalink to “深入理解 LLM 训练中强化学习的关键见解”">​</a></h2><ul><li><strong>PPO-Clip 的核心设计原则：</strong> 为确保训练稳定性，将那些在更新方向上<strong>已取得显著优势</strong>的 token 排除在当前的更新之外。</li><li>在应用于 LLM 的 GRPO 类算法中， PPO-Clip 的关键作用在于 <strong>Token-Masking （Token 掩蔽）机制</strong>，而<strong>重要性采样带来的权重调整在实践中贡献甚微</strong>。</li><li>PPO-Clip 的设计存在<strong>正样本 Token 上的权重失配</strong>问题，这导致训练过程中模型对这些 Token 产生过拟合。进而引发<strong>熵坍缩（entropy collapse）</strong>，最终使模型陷入局部最优。</li><li>流行的 GRPO 算法面临着固有的局限性——<strong>搜索空间受限和奖励粒度粗糙</strong>——这限制了模型的学习能力，使其无法超越基础模型（Base Model）的浅层能力。</li></ul><h1 id="_1-从-ppo-clip-的设计原理说起" tabindex="-1">1. 从 PPO-Clip 的设计原理说起 <a class="header-anchor" href="#_1-从-ppo-clip-的设计原理说起" aria-label="Permalink to “1. 从 PPO-Clip 的设计原理说起”">​</a></h1><p>PPO-Clip 技术由 OpenAI 研究人员在 PPO 算法的设计中引入。它通过对重要性采样比率进行裁剪（Clipping）来约束信任域（Trust Region），从而增强强化学习的稳定性。具体而言，它鼓励策略向有利的方向发展，同时严格限制每一次更新的最大步长。使用 PPO-Clip 的 PPO 损失函数表达式如下：</p><p>$$\\mathcal{L}<em>{\\mathrm{PPO} }(\\theta) = \\mathbb{E}</em>{\\tau}\\bigg[\\min\\Big( r_\\theta(\\tau) A(\\tau),\\mathrm{clip}\\big(r_\\theta(\\tau), 1 - \\varepsilon_{\\mathrm{low} }, 1 +\\varepsilon_{\\mathrm{high} }\\big) A(\\tau) \\Big)\\bigg]$$</p><p>其中：</p><ul><li>$r_\\theta(\\tau) = \\frac{\\pi_\\theta(\\tau)}{\\pi_{\\text{old} }(\\tau)}$</li></ul><p>进一步分析， PPO-Clip 的理论核心可以概括为两个方面：</p><ul><li><strong>Token-Masking （Token 掩蔽）：</strong> 将当前策略下的概率与旧策略偏差过大的 Token 屏蔽（Mask）掉。这确保了每一次更新的“步子不会迈得太大”，从而防止训练崩溃。</li><li><strong>Importance-Sampling （重要性采样）：</strong> 消除采样策略（旧策略）与当前策略之间的分布差异，确保对期望回报的准确估计。因此，模型可以在同一批采样的<strong>数据上进行多次优化更新</strong>，提高样本效率。</li></ul><p>下面展示了 PPO-Clip 的核心实现（摘自 Verl 框架）。在讨论后续概念时，我们将时不时回顾这段代码以帮助理解。接下来，我们将详细探讨 PPO-Clip 是如何实现这两个功能的。</p><p>Python</p><div class="language- line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>negative_approx_kl = torch.clamp(log_prob - old_log_prob, min=-20.0, max=20.0)</span></span>
<span class="line"><span>ratio = torch.exp(negative_approx_kl)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>pg_losses1 = -advantages * ratio</span></span>
<span class="line"><span>pg_losses2 = -advantages * torch.clamp(ratio, 1 - cliprange_low, 1 + cliprange_high)</span></span>
<span class="line"><span>clip_pg_losses1 = torch.maximum(pg_losses1, pg_losses2)</span></span>
<span class="line"><span>pg_clipfrac = verl_F.masked_mean(torch.gt(pg_losses2, pg_losses1).float(), response_mask)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>pg_losses3 = -advantages * clip_ratio_c</span></span>
<span class="line"><span>clip_pg_losses2 = torch.min(pg_losses3, clip_pg_losses1)</span></span>
<span class="line"><span>pg_clipfrac_lower = verl_F.masked_mean(torch.gt(clip_pg_losses1, pg_losses3) * (advantages &lt; 0).float(), response_mask)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>pg_losses = torch.where(advantages &lt; 0, clip_pg_losses2, clip_pg_losses1)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><h2 id="_1-1-token-masking-token-掩蔽" tabindex="-1">1.1 Token-Masking （Token 掩蔽） <a class="header-anchor" href="#_1-1-token-masking-token-掩蔽" aria-label="Permalink to “1.1 Token-Masking （Token 掩蔽）”">​</a></h2><p>我们先来看看 Token 掩蔽，以及 PPO-Clip 究竟掩蔽了哪些 token。这里我们只展示最终的掩蔽效果。关于 PPO 损失公式的详细解析，您可以参考<a href="https://huggingface.co/blog/deep-rl-ppo" target="_blank" rel="noreferrer">这篇指南</a>。结果如下图所示：</p><p>图 1. <strong>标准 PPO 中的裁剪区域。</strong>（来源： <a href="https://arxiv.org/abs/2507.15778%EF%BC%89" target="_blank" rel="noreferrer">https://arxiv.org/abs/2507.15778）</a></p><ul><li>x 轴代表旧策略下的<strong>token 概率</strong>（<code>prob</code>）。</li><li>y 轴代表当前策略下的<strong>token 概率</strong>。</li><li>坐标系中的每个点代表 $f(x, y) = y/x$，即公式中的<strong>重要性比率 $r$</strong>。</li></ul><p>被掩蔽的 Token 可以分为两种情况：</p><ol><li><strong>当 Adv &gt; 0 时：</strong> <strong>$r &gt; 1.2$（$1 + \\text{cliprange_high}$）</strong> 的 Token 被掩蔽（对应左图中的蓝色区域 C）。这些 Token 被排除在梯度计算之外，不参与训练。其余两个区域，即绿色 A 和红色 B 中的 Token 则不做处理——它们的梯度和重要性比率都被保留。其原理是：当 $Adv &gt; 0$ 时，我们沿着左图中箭头的方向增加所有 Token 的概率。但如果某些 Token 的概率已经远高于旧策略（蓝色区域 C），继续增加其概率可能会导致新训练的策略与旧策略偏差过大，从而造成训练不稳定。</li><li><strong>当 Adv &lt; 0 时：</strong> <strong>$r &lt; 0.8$（$1 - \\text{cliprange_low}$）</strong> 的 Token 被掩蔽（对应右图中的红色区域 B）。这是因为当 $Adv &lt; 0$ 时，我们沿着箭头的方向降低这些 Token 的概率。同理，如果当前策略对某个 Token 的概率已经远低于旧策略，进一步降低它可能会导致更新后的策略发生显著偏离，再次危害稳定性。</li></ol><p>此外，当 $Adv&lt;0$ 时，我们仅掩蔽图中下部的 Token （红色区域 B）。然而，在蓝色区域 C 的最左侧部分，当 $x$ 很小而 $y$ 很大时，比率 $y/x$ 会变得极大。这种极端的重要性比率可能会主导回报估计，引起方差爆炸，进一步影响训练稳定性。为了解决这个问题，当 $Adv &lt; 0$ 时，通常会应用<strong>双重裁剪（dual-clip）</strong>，对紫色区域 D 中的 Token 进行掩蔽。在上面的代码中，<code>pg_losses3</code> 部分实现了这个操作（尽管实现上似乎有点问题，我们将在稍后讨论）。</p><p>最终，参与训练的 Token 包括：</p><ul><li>对于 $Adv&gt;0$（左图）：绿色区域 A + 红色区域 B</li><li>对于 $Adv&lt;0$（右图）：绿色区域 A + 蓝色区域 C</li></ul><p>总而言之，我们可以推导出一个<strong>PPO-Clip 的核心设计原则：</strong> <strong>为了确保训练稳定性，将那些在更新方向上已取得显著优势的 Token 排除在当前的训练更新之外。</strong></p><p>此外，我们回顾 <code>verl</code> 的代码实现。虽然代码对所有 Token 的重要性比率 $r$ 都应用了上下界限制（<code>torch.clamp(ratio, 1 - cliprange_low, 1 + cliprange_high)</code>），但在实践中：</p><ul><li>对于<strong>正样本</strong>（$adv &gt; 0$），只有<strong>上界</strong> <code>1 + cliprange_high</code> 是有效的。</li><li>对于<strong>负样本</strong>（$adv &lt; 0$），只有<strong>下界</strong> <code>1 - cliprange_low</code> 是有效的。</li></ul><hr><h2 id="_1-2-importance-sampling-重要性采样" tabindex="-1">1.2 Importance-Sampling （重要性采样） <a class="header-anchor" href="#_1-2-importance-sampling-重要性采样" aria-label="Permalink to “1.2 Importance-Sampling （重要性采样）”">​</a></h2><p><strong>重要性采样的原理</strong>——当难以直接从目标概率分布 $p(x)$ 中采样时，先从更容易采样的替代提议分布 $q(x)$ 中抽取样本。然后，通过计算一个名为“重要性权重”（Importance weight）的比率 $p(x)/q(x)$ 来调整这些样本，以估计函数 $f(x)$ 在 $p(x)$ 下的期望值。</p><p>在 RL 训练中，引入重要性采样的初衷是<strong>提高样本效率</strong>。在标准的策略梯度方法中，通常每个数据样本只用于一次梯度更新。而 PPO 的不同之处在于，在收集了一批采样数据后，它对同一批数据执行<strong>多次优化更新</strong>。虽然这提高了样本利用率，但也引入了采样策略与当前策略分布之间的差异。“重要性权重”就是用来校正这种偏差的。</p><p>从理论上讲，引入重要性采样是完全合理的。然而，早在几年前我刚开始研究 PPO 时，我就有一个疑问：<strong>在 LLM 场景中，重要性权重的作用究竟有多大？</strong> 在 LLM 的 RL 训练中，采样策略与当前策略之间的步数通常很小，学习率也较低。此外，在 KL 和裁剪约束的附加限制下，采样策略与当前策略之间的参数差异通常很微小，这意味着它们的生成分布差异很小。</p><p>那么，重要性权重在实践中的效果究竟有多重要？如果它被证明是微不足道的，这个比率本身是否会引入其他问题？毕竟，除了调整策略分布差异外，重要性权重还有另一个明确——但常常被忽视——的效果：它会<strong>影响每个 Token 的训练权重</strong>。我们将在后续章节中详细分析这一方面。</p><hr><h1 id="_2-grpo-时代下的-ppo-clip-变体" tabindex="-1">2. GRPO 时代下的 PPO-Clip 变体 <a class="header-anchor" href="#_2-grpo-时代下的-ppo-clip-变体" aria-label="Permalink to “2. GRPO 时代下的 PPO-Clip 变体”">​</a></h1><p>今年早些时候，随着 <strong>DeepSeek-R1</strong> 模型的发布，大语言模型（LLM）的强化学习范式逐渐从 PPO 时代过渡到了 GRPO 时代。在过去的六个月中，涌现了各种基于 GRPO 的算法，其中著名的例子包括 <strong>Seed 的 DAPO</strong>、<strong>Minimax 的 CISPO</strong> 和 <strong>Qwen 的 GSPO</strong>。</p><p>GRPO 类算法与 PPO 的核心区别在于<strong>优势值（Advantage）的计算方式。PPO 使用 Token-level Advantage （Token 级别的优势值）</strong>，其中每个 Token 都分配有一个单独的优势值，通过价值模型和奖励模型分别计算。相比之下， GRPO 类算法使用<strong>Response-level Advantage （响应级别的优势值）</strong>：对于给定的一个提示，采样多个响应，并使用以下公式计算这组响应的标准化得分。这个单一得分随后被分配为该响应内所有 token 的统一优势值。换句话说，<strong>同一响应内的所有 Token 共享相同的优势值</strong>，没有 Token 级别的差异化。</p>`,37),n("p",null,[e("$$\\hat{A}"),n("em",{j:"1"},"{i,t} = \\frac{R_i - \\mathrm{mean}\\left({R_j}"),e("^G\\right)}{\\mathrm{std}\\left({R_j}_{j=1}^G\\right)}$$")],-1),o('<p>接下来，我们将从<strong>Token-Masking</strong>和<strong>Importance-Sampling</strong>这两个关键角度，系统回顾各种 GRPO 算法对 PPO-Clip 所做的改进。</p><hr><h2 id="_2-1-token-masking-token-掩蔽-的相关改进" tabindex="-1">2.1 Token-Masking （Token 掩蔽）的相关改进 <a class="header-anchor" href="#_2-1-token-masking-token-掩蔽-的相关改进" aria-label="Permalink to “2.1 Token-Masking （Token 掩蔽）的相关改进”">​</a></h2><p>PPO-Clip 引入的 Token 掩蔽机制无疑有助于训练的稳定性。如前所述，该机制可以防止在更新方向上已获得较大优势的 Token 参与训练。通过这样做，它确保了更新后的策略不会与旧策略偏离过远，从而有效地维护了训练的稳定性。大量的实验验证逐渐强化了人们的认识：<strong>该机制对于稳定强化学习训练至关重要</strong>。它的设计简单、高效、真正优雅——一个绝妙的设计！</p><p>在过去的六个月中，出现了一些完善 Token 掩蔽细节的工作。这些方法都属于 GRPO 算法家族。根据其修改策略，大致可以归类如下：</p><h3 id="_2-1-1-修改裁剪范围" tabindex="-1">2.1.1 修改裁剪范围 <a class="header-anchor" href="#_2-1-1-修改裁剪范围" aria-label="Permalink to “2.1.1 修改裁剪范围”">​</a></h3><p><strong>Clip-Higher</strong></p><p>这类方法的代表是 DAPO 中提出的 <em>clip-higher</em> 策略。DAPO 的研究人员指出，原始的上界裁剪阈值在一定程度上会限制低概率 Token 的概率增长，可能限制模型生成的多样性。通过<strong>提高上界裁剪限制</strong>（即代码中的 <code>cliprange_high</code> 变量），该方法为提升低概率 Token 的概率提供了更多空间。</p><p>需要注意的是，<code>cliprange_high</code> 仅对<strong>正样本</strong>——即具有正优势值的 Token ——产生影响。换句话说，<em>clip-higher</em> 策略有效地<strong>扩大了正样本的裁剪范围</strong>，而负样本的裁剪范围保持不变。本质上，该方法沿着优势值维度对 token 进行了分类，并相应地调整了裁剪范围，如下图所示。</p><p>图 2. <strong>DAPO Clip-Higher 策略中的裁剪区域。</strong></p><p>然而，&quot;限制低概率 Token 增长&quot;的根本原因实际上在于 Token 本身，并不局限于正样本。事实上，低概率 Token 在正样本和负样本中都可能存在。这种改进没有考虑到负样本中的低概率 Token，同时却扩大了正样本中高概率 Token 的概率增长。这可能会<strong>加速策略熵的下降</strong>，并增加模型偏离基线的风险。</p><p>尽管 <em>clip-higher</em> 仍然是一种不完美的改进策略，但它是第一个明确指出 PPO-Clip 裁剪范围局限性并提出针对性调整的方法，提供了重要的概念启示——绝对是一个值得注意的贡献。</p><p><strong>Dual-Token-Clip</strong></p><p>为了解决上述问题，最近的一项工作提出了在 <strong>Token 级别</strong>区分裁剪范围：低概率 Token 使用较大的裁剪范围，而高概率 Token 使用较小的范围。这种方法可以防止高概率 Token 干扰整体更新范围，从而允许将裁剪范围设置得更大。例如，在 DAPO 中，<em>clip-higher</em> 值通常设置为 0.28，而在 Dual-Token-Clip 策略中，它可以放宽到 0.4 甚至更高。实验结果表明，该策略在训练稳定性和最终性能上均优于 <em>clip-higher</em>。</p><p>此外，如下图所示， Dual-Token-Clip 策略定义了两个独立的调整范围（E 和 F），分别对应正优势和负优势区域。由于计算限制，目前尚未完全探索每个范围对训练的具体影响和机制。（<strong>TODO：</strong> 在未来的更新中进行消融实验并提供进一步分析。）</p><p>图 3. <strong>Dual-Token-Clip 策略中的裁剪区域。</strong></p><p><strong>Dynamic-Clip</strong></p><p>在 Dual-Token-Clip 工作的基础上，我们进一步思考：如果 Token 可以被划分为两种类型并分别施加约束，我们是否可以走得更远，更精细地调整裁剪范围？这导致了为每个 Token 分配一个独立的裁剪范围，实现 <strong>Token 级动态裁剪</strong>的想法。这项工作目前正在进行中，初步实验显示出积极的结果。然而，目前的设计在机制上还不够简单和优雅，尚未达到我们理想的状态。因此，目前不进行公开分享，可能会在随后的正式技术报告中包含。</p><p>最近的一项工作，<em><a href="https://arxiv.org/abs/2509.02333" target="_blank" rel="noreferrer">DCPO: Dynamic Clipping Policy Optimization</a></em> ，提出了一个相似的优化方向，也专注于动态调整裁剪机制，尽管具体设计有所不同。</p><h3 id="_2-1-2-soft-clip-策略" tabindex="-1">2.1.2 Soft Clip 策略 <a class="header-anchor" href="#_2-1-2-soft-clip-策略" aria-label="Permalink to “2.1.2 Soft Clip 策略”">​</a></h3><p>在 Token 裁剪过程中，有些方法保留了梯度，而只限制重要性权重的大小。一个代表性的例子是 Minimax 提出的<strong>CISPO</strong>。其损失函数定义如下：</p>',21),n("p",null,[e("$$\\mathcal{J}"),n("em",null,"{\\text{CISPO} }(\\theta) = \\mathbb{E}"),e("{(q,a)\\sim \\mathcal{D}, {o_i}"),n("em",null,"{i=1}^G \\sim \\pi"),e("{\\theta_{\\text{old} } }(\\cdot \\mid q)} \\left[ \\frac{1}{\\sum_{i=1}^G |o_i|} \\sum_{i=1}^G \\sum_{t=1}^{|o_i|} \\text{sg}(\\hat{r}"),n("em",{"i,t":""},"{i,t}(\\theta)) \\hat{A}"),e(" \\log \\pi_\\theta(o_{i,t} \\mid q, o_{i,<t}) \\right],$$")],-1),n("p",null,"其中：",-1),n("p",null,[e("$$\\hat{r}"),n("em",{"i,t":""},"{i,t}(\\theta) = \\text{clip}!\\left( r"),e("(\\theta), , 1-\\epsilon^{IS}"),n("em",null,"{\\text{low} }, , 1+\\epsilon^{IS}"),e("{\\text{high} } \\right).$$")],-1),o('<p>与 PPO-Clip 相比， CISPO 有两个主要区别：首先， CISPO 没有使用 PPO 的单边裁剪机制。相反，无论优势值是正还是负，它都对上界和下界进行裁剪。其次，在裁剪之后， CISPO 没有掩蔽（Mask） Token，而是<strong>保留了其梯度</strong>，使其继续参与训练，同时将超出范围的重要性权重裁剪到预设的边界（通常上界为 1.2，下界为 0.8）。我们将这种机制称为<strong>Soft Clip 策略</strong>。</p><p>当 CISPO 首次发表时，我们受到启发，思考这种 Soft Clip 机制是否可以直接应用于 PPO-Clip 的单边裁剪。具体来说，对于 <code>advantage &gt; 0</code> 且超出上界，以及 <code>advantage &lt; 0</code> 且低于下界的 Token，我们保留梯度进行更新，而仅将重要性权重设置为 1.2 或 0.8。然而，仔细检查后，发现了两个问题：</p><ol><li><strong>违反 PPO-Clip 的设计初衷：</strong> PPO-Clip 旨在掩蔽在更新方向上已具有显著优势的 Token，以防止策略更新步长过大，确保训练稳定性。使用 Soft Clip 允许这些 token 继续参与更新，这很容易导致训练崩溃。在一个 1.5B 模型上，在相对较大的离策略设置（<code>ppo_epochs=3</code>，<code>batch_size / mini_batch_size = 4</code>）的对比实验中， PPO-Clip 保持稳定，而使用 Soft Clip 则在大约 100 步左右崩溃。</li><li><strong>重要性权重失配：</strong> 裁剪后， Token 的重要性权重被统一设置为 0.8 或 1.2，这改变了原始的重要性采样比率。虽然理论上这构成了失配，但其在实践中的影响可能有限。</li></ol><p>这两个问题也存在于标准的 CISPO 算法中。因此，我们得出结论，<strong>不建议将 Soft Clip 直接应用于 PPO-Clip 的裁剪机制</strong>。尽管如此，这种策略仍然可以应用于其他上下文，我们将在稍后讨论。与 DAPO 类似，尽管存在这些问题， Minimax 仍值得认可，因为它是第一个将 Soft Clip 机制引入 PPO-Clip 的工作。</p><h3 id="_2-1-3-response-level-clip-响应级别裁剪" tabindex="-1">2.1.3 Response-level Clip （响应级别裁剪） <a class="header-anchor" href="#_2-1-3-response-level-clip-响应级别裁剪" aria-label="Permalink to “2.1.3 Response-level Clip （响应级别裁剪）”">​</a></h3><p>目前，只有<strong>GSPO</strong>采用了这种裁剪策略。与其他进行 Token 级别裁剪的方法不同， GSPO 在<strong>响应级别</strong>进行裁剪：它首先计算一个响应中所有 Token 的平均重要性权重，作为该响应的代表性权重。如果这个平均值与预设范围显著偏离，则<strong>整个响应被丢弃</strong>。</p><p>当我第一次接触这个策略时，我立即质疑其合理性。</p><p>在传统的 Token 级别裁剪中，被裁剪的 Token 比例通常非常低。例如，在我对 1.5B 模型的实验中，裁剪比率通常低于 0.001。换句话说，在一个包含约 20,000 个 Token 的响应中，通常只有大约 20 个 Token ——那些具有极端偏差的 Token （正样本大于 1.2 或负样本小于 0.8）——被裁剪，而绝大多数 token 仍在合理范围内。</p><p>GSPO 采用的响应级别裁剪策略可能存在以下问题：</p><p>首先，使用整个响应的平均重要性权重作为裁剪标准意味着少数<strong>极端值很容易夸大或缩小平均值</strong>，导致整个响应被错误地丢弃。在许多情况下，裁剪少量异常 Token 就足以纠正响应。然而， GSPO 的方法是<strong>完全丢弃所有信息</strong>，降低了采样数据的利用率，造成浪费。</p><p>其次，即使在极端值较少、整体权重相对稳定的响应中， GSPO 仍然保留所有 Token 进行训练。这显然违反了 PPO-Clip 引入 Token 掩蔽的初衷——即掩蔽在<strong>优势更新方向上显著偏离原始策略</strong>的 Token。继续使用这些 token 可能反而会破坏模型训练的稳定性。</p><p>此外，论文提到 GSPO 裁剪的 Token 数量比 GRPO 多约 100 倍。这并不奇怪： Token 级别的裁剪比率极低（约 0.1% ），而在响应级别方法中，只要一组中有一个响应被完全丢弃，裁剪比率就会急剧增加。具体来说，如果一个提示对应 G 个响应，裁剪比率将扩大约 1k / G 倍。</p><p>基于此分析，我们认为 GSPO 粗粒度的<strong>响应级别裁剪机制是不合理的</strong>。</p><p>至于 GSPO 的另一个修改——在响应级别计算重要性权重以与奖励信号的粒度对齐——我们暂不讨论其有效性。假设该机制是有效的，那么对标准 GSPO 更合理的改进将是：</p><ul><li>保持 <strong>Token 级别裁剪</strong>，以精细控制策略更新；</li><li>仅基于裁剪后剩余的 Token 计算<strong>响应级别的重要性权重</strong>，并将其用于平均值计算。</li></ul><hr><h2 id="_2-2-importance-sampling-重要性采样-的改进" tabindex="-1">2.2 Importance-Sampling （重要性采样）的改进 <a class="header-anchor" href="#_2-2-importance-sampling-重要性采样-的改进" aria-label="Permalink to “2.2 Importance-Sampling （重要性采样）的改进”">​</a></h2><p>到目前为止，唯一修改重要性采样的似乎只有 GSPO。该论文首先指出了 GRPO 算法中的一个<strong>根本性问题</strong>：<strong>奖励信号的粒度与重要性采样的粒度不匹配</strong>。如前所述， GRPO 类算法中的优势值是在<strong>响应级别</strong>计算的，这意味着同一响应内的所有 Token 共享相同的优势值。然而，重要性采样却是在 <strong>Token 级别</strong>执行的，每个 Token 都乘以其对应的重要性权重来校正分布。</p><p>GSPO 的作者认为，这种 Token 级别的重要性采样会引入误差，随着响应长度的增加而累积，最终导致训练不稳定甚至模型崩溃。原始论文中指出：</p><p>为了解决这个问题， GSPO 修改了 GRPO 中的重要性采样机制，使其与<strong>响应级别奖励</strong>对齐。具体来说，它平均了同一响应中所有 Token 的重要性权重，以获得一个代表整个响应的单一权重。修改后的 GSPO 损失函数定义如下：</p><p>但是，切换到响应级别权重是否合理？我们在这里不急于下结论，将在后续章节中更详细地探讨。现在，我们首先比较实验结果。</p><p>为了清楚地评估响应级别权重的影响，我们进行了对照实验，同时保留了原始 PPO-Clip 中的 Token 掩蔽机制。我们设置了两个实验组：一个使用 GRPO 的<strong>Token 级别重要性权重</strong>，另一个使用 GSPO 的<strong>响应级别权重</strong>，所有其他设置都相同。大多数训练指标显示两者趋势相似，仅在熵上略有差异——使用 GSPO 权重的模型表现出<strong>略快的熵衰减</strong>。结果如下：</p><p>图 4. 训练过程中的 Actor/熵： GSPO vs. GRPO。</p><p>图 5. 训练过程中 LiveCodeBench 上的性能： GSPO vs. GRPO。</p><p>从结果来看，使用 GSPO 的响应级别重要性权重确实<strong>加速了收敛</strong>，测试集性能上升更快，这与论文中的说法一致。</p><p>然而，在快速达到峰值性能后， GSPO 也<strong>更早开始下降</strong>，最终收敛结果与 GRPO 大致相当。在峰值性能方面， GSPO 没有表现出显著优势。事实上， GSPO 论文中报告的实验主要显示了更快的训练速度，但没有提供证据证明最终性能超越了 GRPO。</p><p>我们试图分析响应级别权重可能加速学习的原因：在该机制下，响应中的所有 Token 不仅共享相同的优势值，还共享相同的重要性权重。这意味着在更新过程中，一个 Token 的权重完全由其概率决定——<strong>低概率 Token 具有更大的梯度，从而主导优化</strong>。本质上，这为低概率（高熵） Token 分配了更高的优化权重。一些研究表明， RL 训练主要优化高熵 Token，因此 GSPO 的机制可能确实通过关注它们来加速学习。然而，这种加速不一定能转化为更好的最终性能，且收敛结果不能保证优于原始 GRPO 方法。</p><hr><h1 id="_3-importance-sampling-is-not-important-重要性采样并不重要" tabindex="-1">3. Importance Sampling is not Important （重要性采样并不重要） <a class="header-anchor" href="#_3-importance-sampling-is-not-important-重要性采样并不重要" aria-label="Permalink to “3. Importance Sampling is not Important （重要性采样并不重要）”">​</a></h1><h2 id="_3-1-动机" tabindex="-1">3.1 动机 <a class="header-anchor" href="#_3-1-动机" aria-label="Permalink to “3.1 动机”">​</a></h2><p>重要性采样（IS）是估计一个概率分布下期望值，同时从另一个分布采样的基础技术。在 RL 中，这种方法很有价值，因为当前策略的轨迹分布通常与旧策略不同。通过使用重要性比率重新加权回报，可以重用旧策略下收集的数据，从而以更高的样本效率支持离策略学习（Off-Policy Learning）。</p><p>在 LLM 中基于结果的 RL （如 GRPO）的背景下，同一响应内的所有 Token 共享相同的优势值。然而，如果我们考察单个 Token，并考虑其对最终正确性的实际贡献， GRPO 分配的优势值可能存在两个问题：（1）它在数值上可能不准确，因为不同 Token 对最终答案正确性的贡献可能不均等；（2）更新方向可能产生误导，因为一个最终答案正确的响应仍可能包含不正确的中间步骤。</p><p>引入 IS 的初衷是通过校正分布失配来更好地估计期望的 Token 级别回报。然而，这引出了以下问题：</p><ul><li><strong>如果由于基于结果的优势值估计，分配给每个 Token 的奖励已经不准确，那么使用 IS 权重进一步调整分布的重要性有多大？</strong></li></ul><p>与 GSPO 将重要性权重统一到响应级别的方法不同，我一直在思考一个更根本的问题：在 LLM 的 RL 背景下——或者更谨慎地说，在将 GRPO 类算法应用于 LLM 训练时——<strong>重要性采样到底有多重要？</strong> 这种分布校正机制在实践中究竟贡献了多少？</p><hr><h2 id="_3-2-初步实验" tabindex="-1">3.2 初步实验 <a class="header-anchor" href="#_3-2-初步实验" aria-label="Permalink to “3.2 初步实验”">​</a></h2><p>为了研究 IS 权重在基于结果的 RL 中的实际效果，我们设计了两种方法进行比较。两种方法使用相同的 Token 掩蔽策略，但在 IS 权重上有所不同：</p><ul><li><strong>方法 1：</strong> 带有原始 IS 的 GRPO，即标准 PPO-Clip。</li><li><strong>方法 2：</strong> 不带 IS 的 GRPO （所有 IS 权重固定为 1.0）。</li></ul><p>为了更清楚地观察 IS 的效果，实验采用了相对较高的离策略更新程度：<code>ppo_epochs = 3</code>，<code>batch_size / mini_batch_size = 4</code>。所有后续实验均遵循此配置。</p><p>图 6. <strong>标准 GRPO （蓝色）与不带 IS 的 GRPO （橙色）在测试准确度和训练动态方面的比较。</strong></p><p>如上图所示，两种方法在测试集上的总体性能差异很小。然而，<strong>训练指标</strong>（如熵、重复率、截断率和 KL 散度）在<strong>没有 IS 的情况下变化更平稳</strong>，尤其是在训练的后期阶段。仔细观察可以发现，在训练接近尾声时，两种方法在测试集上的指标也略有分歧。标准 GRPO 更早达到峰值，然后开始下降，而另一种方法收敛更平稳。它维持在接近峰值的值附近，没有出现下降趋势。尽管 GRPO 的峰值略高于不带 IS 的 GRPO，但差异仅约为 0.4 个百分点。这在 LLM 训练和评估的随机波动范围内。</p><p>接下来，我们分析造成这种差异的原因。</p><p>图 7. <strong>GRPO 训练过程中响应级别裁剪比率的演变。</strong></p><p>首先，需要注意关于 GRPO 训练的一些普遍观察结果：</p><ol><li>当使用 GRPO 类算法训练 LLM 时，<strong>一个响应的平均重要性采样权重通常略大于 1.0</strong>。例如，在我对 1.5B 模型的实验中，它通常在 1.0005 左右。</li><li>此外，<strong>正样本的平均权重略高于负样本</strong>，如上图所示。尽管这种差异看起来很微小，但当对数万个 token 求平均时，它的实际影响是显著的。正负样本权重之间的这种微妙差异导致 GRPO 训练<strong>更倾向于拟合正样本，而不是抑制负样本</strong>。在训练过程中，这种差距逐渐扩大，加速了正样本的拟合，从而加速了熵衰减的速度。（这与 GRPO 算法——或更广泛地说， PPO-Clip 设计——中固有的局限性有关，我们将在后续文章中详细讨论。）</li></ol><p>为了进一步阐明熵更快衰减与正样本加权之间的关系，有必要了解<strong>熵与正/负样本之间的关系</strong>：在 GRPO 训练中，采样更多负例（例如，来自更难的提示）或手动增加负样本权重会<strong>减缓熵衰减</strong>，甚至可能导致熵暂时上升——尽管模型在测试集上的性能通常保持不变或略微恶化。相反，<strong>增加正样本的权重会加速熵衰减</strong>，通常导致最终性能略差。将这一观察结果与先前关于熵机制的研究（例如，来自上海 AI Lab 的研究）相结合，我们可以进一步得出结论：在 GRPO 训练中，观察到的训练熵变化可能主要由<strong>低熵 Token</strong>驱动，这种熵可能无法准确反映高熵 Token 上的探索。</p><p>最后，为什么<strong>正样本的平均权重高于负样本</strong>？这与模型预期的学习行为一致：经过几轮训练后，当前策略为正样本分配的生成概率高于旧策略，导致裁剪比率分布向<strong>左上方</strong>区域移动。相反，负样本低于旧策略，将裁剪比率移向<strong>右下方</strong>区域。这表明模型正朝着正确的方向学习，逐渐区分正负样本的预测概率。</p>',48)])])}const m=a(p,[["render",l]]);export{_ as __pageData,m as default};
